export type OTChar = { id: string; ch: string };

export type ZTToken = { id: string; text: string };

export type KeysPerOTMode = 'single' | 'multiple';

export type LockedKeys = Record<string, string>;
